/**
 * IconButton - Molecule Component
 *
 * @description
 * Alias export for StrapiIconButton to maintain consistent naming convention
 * across the molecules folder. This allows components to import from molecules
 * without mixing element/molecule namespaces.
 *
 * @remarks
 * - Server Component (uses Next.js Link, no hooks/state)
 * - Renders a button with icon and label
 * - Supports variants: default, outline, ghost
 * - Handles external links with target="_blank"
 */

export { StrapiIconButton as IconButton } from "@/components/page-builder/components/elements/StrapiIconButton"
